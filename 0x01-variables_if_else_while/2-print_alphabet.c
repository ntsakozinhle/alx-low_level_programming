#include <stdio.h>
/**
 * main - this program prints the alphabet in
 * lowercase
 *
 * cannot use printf
 *
 * Return: Always 0
 */
int main(void)
{
	char alpha;

	for(alpha = 'a' ; alpha <= 'z' ; alpha++)
	
	{
		putchar(alpha);
	}
	
	return (0);
}
