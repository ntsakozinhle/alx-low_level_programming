#include "main.h"
#include <stdlib.h>

/**
 * main - a program that prints its name, followed by a new line
 *
 * Return: void
 */

int main(int argc, char *argv[])
{
	int i;

	printf("%s\n", argv[i]);

	return (0);
}
