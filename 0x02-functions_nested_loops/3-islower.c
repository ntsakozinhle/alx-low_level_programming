#include <stdio.h>
#include "main.h"

/**
 * _islower - writes a function that checks for lowercase
 * characters
 *
 * @x :parameter to be checked
 *
 * Return: 1 or 0
 */

int _islower(int x)
{
		if (x >= 'a' && x <= 'z')
			return (1);
		else
			return (0)
}
